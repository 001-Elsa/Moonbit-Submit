name = "local/moonpack"

version = "0.1.0"

readme = "README.md"

repository = ""

license = "Apache-2.0"

keywords = [
  "serialization",
  "schema",
  "codegen",
  "wire-format",
]

description = "MoonPack is a MoonBit-native schema-first binary serialization toolkit."

import {
  "moonbitlang/x@0.4.45"
}
