# MoonPack 项目申报书

## 基本信息

| 项目 | 内容 |
| --- | --- |
| 项目名称 | MoonPack：MoonBit 原生 Schema-First 二进制序列化工具包 |
| 参赛者 | 001-Elsa |
| 联系方式 | 以报名系统登记信息为准 |
| GitHub 仓库链接 | https://github.com/001-Elsa/Moonbit-Submit |
| Gitlink 仓库链接 | https://gitlink.org.cn/Hanzzz/MoonPack_Hz |
| Mooncakes 包 | `001-Elsa/moonpack@0.1.1` |
| 项目方向 | MoonBit 基础设施 / 开发者工具 / 数据序列化 |
| 项目类型 | 原创项目 |

## 项目简介

MoonPack 是一个面向 MoonBit 生态的 schema-first 二进制序列化工具包。项目提供 `.mpack` Schema 语言、解析与校验能力、版本兼容性检查、MoonBit 代码生成、命令行工具和示例工程，覆盖从 Schema 定义到二进制编解码与测试验证的完整工作流。

项目目标是在 MoonBit 中提供一个比 protobuf 更轻量、更易审阅、更易扩展的原生序列化基础设施，适合工具链缓存、游戏存档、本地配置、小型服务数据交换和测试夹具生成等场景。

## 核心功能

- `.mpack` Schema 语言：支持 `message`、`enum`、`List[T]`、`T?`、`reserved`、`deprecated`。
- Schema 工具链：提供 lexer、parser、validator 和 `compat` 兼容性检查。
- 二进制 Wire Format：支持 varint、fixed64、length-delimited 和未知字段跳过。
- MoonBit 代码生成：生成结构体、枚举、默认值、样例值、相等比较、编码/解码函数和 round-trip 测试。
- CLI 命令：提供 `check`、`compat`、`gen`、`doc`。
- 示例与文档：提供 auth、savegame、compat、invalid 示例，以及 schema/wire format 文档。

## 原创 / 参考说明

本项目为原创项目，不是移植项目。设计上参考了 Google Protocol Buffers 的字段编号与 wire type 思路，但 Schema 语法、MoonBit 代码生成、兼容性检查和工具链实现均为面向 MoonBit 的原生设计；二进制格式不追求与 protobuf 兼容。

参考项目：

- Protocol Buffers: https://github.com/protocolbuffers/protobuf
- 参考许可证：BSD 3-Clause License

## 交付与验收证据

- 本地 `moon check` 通过。
- 本地 `moon build` 通过。
- 本地 `moon test` 通过，当前为 43 个测试全部通过。
- GitHub Actions 最新 CI 通过：https://github.com/001-Elsa/Moonbit-Submit/actions/runs/29161109332
- GitHub 与 Gitlink 均已同步到最新提交。
- 已发布到 mooncakes.io，可通过 `moon fetch 001-Elsa/moonpack@0.1.1` 拉取。
- 根目录包含 Apache-2.0 `LICENSE`。

## 提交材料信息

提交报名材料时可附以下信息：

- GitHub: https://github.com/001-Elsa/Moonbit-Submit
- Gitlink: https://gitlink.org.cn/Hanzzz/MoonPack_Hz
- CI 通过链接: https://github.com/001-Elsa/Moonbit-Submit/actions/runs/29161109332
- Mooncakes 包: `001-Elsa/moonpack@0.1.1`
- 验证命令: `moon fetch 001-Elsa/moonpack@0.1.1`

## 仓库说明

GitHub 仓库与 Gitlink 仓库提交内容保持同步。Gitlink 默认分支 `master` 和 `main` 均指向当前验收提交，避免评审从默认分支进入时看到旧内容。
