// Generated by MoonPack. Do not edit by hand.

///|
test "moonpack QuestObjective default roundtrip" {
  let value = default_quest_objective()
  let bytes = encode_quest_objective(value)
  match decode_quest_objective(bytes) {
    Ok(decoded) =>
      if !equal_quest_objective(value, decoded) {
        fail("roundtrip value mismatch")
      }
    Err(_) => fail("roundtrip failed")
  }
}

///|
test "moonpack QuestObjective sample roundtrip" {
  let value = sample_quest_objective()
  let bytes = encode_quest_objective(value)
  match decode_quest_objective(bytes) {
    Ok(decoded) =>
      if !equal_quest_objective(value, decoded) {
        fail("sample roundtrip value mismatch")
      }
    Err(_) => fail("sample roundtrip failed")
  }
}
