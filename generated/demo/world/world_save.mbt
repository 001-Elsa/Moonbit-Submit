// Generated by MoonPack. Do not edit by hand.
// Package importing this file should import 001-Elsa/moonpack/src/core as @core.

///|
pub struct WorldSave {
  save_id : String
  version : Int
  clock : WorldClock
  players : Array[PlayerProfile]
  regions : Array[RegionState]
  quests : Array[QuestLogEntry]
  debug_note : String?
}

///|
pub fn default_world_save() -> WorldSave {
  {
    save_id: "",
    version: 0,
    clock: default_world_clock(),
    players: [],
    regions: [],
    quests: [],
    debug_note: None,
  }
}

///|
pub fn sample_world_save() -> WorldSave {
  {
    save_id: "sample_save_id",
    version: 42,
    clock: sample_world_clock(),
    players: [sample_player_profile()],
    regions: [sample_region_state()],
    quests: [sample_quest_log_entry()],
    debug_note: Some("sample_debug_note"),
  }
}

///|
pub fn equal_world_save(lhs : WorldSave, rhs : WorldSave) -> Bool {
  if lhs.save_id != rhs.save_id {
    return false
  }
  if lhs.version != rhs.version {
    return false
  }
  if !equal_world_clock(lhs.clock, rhs.clock) {
    return false
  }
  if lhs.players.length() != rhs.players.length() {
    return false
  }
  let mut i_players = 0
  while i_players < lhs.players.length() {
    if !equal_player_profile(lhs.players[i_players], rhs.players[i_players]) {
      return false
    }
    i_players = i_players + 1
  }
  if lhs.regions.length() != rhs.regions.length() {
    return false
  }
  let mut i_regions = 0
  while i_regions < lhs.regions.length() {
    if !equal_region_state(lhs.regions[i_regions], rhs.regions[i_regions]) {
      return false
    }
    i_regions = i_regions + 1
  }
  if lhs.quests.length() != rhs.quests.length() {
    return false
  }
  let mut i_quests = 0
  while i_quests < lhs.quests.length() {
    if !equal_quest_log_entry(lhs.quests[i_quests], rhs.quests[i_quests]) {
      return false
    }
    i_quests = i_quests + 1
  }
  match lhs.debug_note {
    None =>
      match rhs.debug_note {
        None => ()
        _ => return false
      }
    Some(left) =>
      match rhs.debug_note {
        Some(right) => if left != right { return false }
        _ => return false
      }
  }
  true
}

///|
pub fn encode_world_save(value : WorldSave) -> Array[Byte] {
  let writer = @core.Writer::new()
  @core.write_string_field(writer, 1, value.save_id)
  @core.write_int_field(writer, 2, value.version)
  @core.write_message_field(writer, 3, encode_world_clock(value.clock))
  for item in value.players {
    @core.write_message_field(writer, 4, encode_player_profile(item))
  }
  for item in value.regions {
    @core.write_message_field(writer, 5, encode_region_state(item))
  }
  for item in value.quests {
    @core.write_message_field(writer, 6, encode_quest_log_entry(item))
  }
  match value.debug_note {
    Some(v) => @core.write_string_field(writer, 7, v)
    None => ()
  }
  writer.finish()
}

///|
pub fn decode_world_save(
  input : Array[Byte],
) -> Result[WorldSave, @core.DecodeError] {
  let reader = @core.Reader::new(input)
  let mut save_id : String = ""
  let mut version : Int = 0
  let mut clock : WorldClock = default_world_clock()
  let players : Array[PlayerProfile] = []
  let regions : Array[RegionState] = []
  let quests : Array[QuestLogEntry] = []
  let mut debug_note : String? = None
  while !reader.is_empty() {
    match @core.read_key(reader) {
      Err(err) => return Err(err)
      Ok(key) =>
        match key.number {
          1 =>
            match @core.read_string_value(reader, key.wire_type) {
              Err(err) => return Err(err)
              Ok(v) => save_id = v
            }
          2 =>
            match @core.read_int_value(reader, key.wire_type) {
              Err(err) => return Err(err)
              Ok(v) => version = v
            }
          3 =>
            match @core.read_message_value(reader, key.wire_type) {
              Err(err) => return Err(err)
              Ok(bytes) =>
                match decode_world_clock(bytes) {
                  Ok(v) => clock = v
                  Err(err) => return Err(err)
                }
            }
          4 =>
            match @core.read_message_value(reader, key.wire_type) {
              Err(err) => return Err(err)
              Ok(bytes) =>
                match decode_player_profile(bytes) {
                  Ok(v) => players.push(v)
                  Err(err) => return Err(err)
                }
            }
          5 =>
            match @core.read_message_value(reader, key.wire_type) {
              Err(err) => return Err(err)
              Ok(bytes) =>
                match decode_region_state(bytes) {
                  Ok(v) => regions.push(v)
                  Err(err) => return Err(err)
                }
            }
          6 =>
            match @core.read_message_value(reader, key.wire_type) {
              Err(err) => return Err(err)
              Ok(bytes) =>
                match decode_quest_log_entry(bytes) {
                  Ok(v) => quests.push(v)
                  Err(err) => return Err(err)
                }
            }
          7 =>
            match @core.read_string_value(reader, key.wire_type) {
              Err(err) => return Err(err)
              Ok(v) => debug_note = Some(v)
            }
          _ =>
            match @core.skip_field(reader, key.wire_type) {
              Ok(_) => ()
              Err(err) => return Err(err)
            }
        }
    }
  }
  Ok({ save_id, version, clock, players, regions, quests, debug_note })
}
