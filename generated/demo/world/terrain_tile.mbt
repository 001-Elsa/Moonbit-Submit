// Generated by MoonPack. Do not edit by hand.
// Package importing this file should import 001-Elsa/moonpack/src/core as @core.

///|
pub struct TerrainTile {
  tile_id : Int
  kind : TerrainKind
  elevation : Double
  resources : Array[ResourceStack]
}

///|
pub fn default_terrain_tile() -> TerrainTile {
  { tile_id: 0, kind: Plains, elevation: 0.0, resources: [] }
}

///|
pub fn sample_terrain_tile() -> TerrainTile {
  {
    tile_id: 42,
    kind: Forest,
    elevation: 3.5,
    resources: [sample_resource_stack()],
  }
}

///|
pub fn equal_terrain_tile(lhs : TerrainTile, rhs : TerrainTile) -> Bool {
  if lhs.tile_id != rhs.tile_id {
    return false
  }
  if terrain_kind_to_int(lhs.kind) != terrain_kind_to_int(rhs.kind) {
    return false
  }
  if lhs.elevation != rhs.elevation {
    return false
  }
  if lhs.resources.length() != rhs.resources.length() {
    return false
  }
  let mut i_resources = 0
  while i_resources < lhs.resources.length() {
    if !equal_resource_stack(
        lhs.resources[i_resources],
        rhs.resources[i_resources],
      ) {
      return false
    }
    i_resources = i_resources + 1
  }
  true
}

///|
pub fn encode_terrain_tile(value : TerrainTile) -> Array[Byte] {
  let writer = @core.Writer::new()
  @core.write_int_field(writer, 1, value.tile_id)
  @core.write_int_field(writer, 2, terrain_kind_to_int(value.kind))
  @core.write_double_field(writer, 3, value.elevation)
  for item in value.resources {
    @core.write_message_field(writer, 4, encode_resource_stack(item))
  }
  writer.finish()
}

///|
pub fn decode_terrain_tile(
  input : Array[Byte],
) -> Result[TerrainTile, @core.DecodeError] {
  let reader = @core.Reader::new(input)
  let mut tile_id : Int = 0
  let mut kind : TerrainKind = Plains
  let mut elevation : Double = 0.0
  let resources : Array[ResourceStack] = []
  while !reader.is_empty() {
    match @core.read_key(reader) {
      Err(err) => return Err(err)
      Ok(key) =>
        match key.number {
          1 =>
            match @core.read_int_value(reader, key.wire_type) {
              Err(err) => return Err(err)
              Ok(v) => tile_id = v
            }
          2 =>
            match @core.read_int_value(reader, key.wire_type) {
              Err(err) => return Err(err)
              Ok(v) => kind = terrain_kind_from_int(v)
            }
          3 =>
            match @core.read_double_value(reader, key.wire_type) {
              Err(err) => return Err(err)
              Ok(v) => elevation = v
            }
          4 =>
            match @core.read_message_value(reader, key.wire_type) {
              Err(err) => return Err(err)
              Ok(bytes) =>
                match decode_resource_stack(bytes) {
                  Ok(v) => resources.push(v)
                  Err(err) => return Err(err)
                }
            }
          _ =>
            match @core.skip_field(reader, key.wire_type) {
              Ok(_) => ()
              Err(err) => return Err(err)
            }
        }
    }
  }
  Ok({ tile_id, kind, elevation, resources })
}
