// Generated by MoonPack. Do not edit by hand.
// Package importing this file should import 001-Elsa/moonpack/src/core as @core.

///|
pub struct QuestLogEntry {
  quest_id : String
  state : QuestState
  objectives : Array[QuestObjective]
  note : String?
}

///|
pub fn default_quest_log_entry() -> QuestLogEntry {
  { quest_id: "", state: Locked, objectives: [], note: None }
}

///|
pub fn sample_quest_log_entry() -> QuestLogEntry {
  {
    quest_id: "sample_quest_id",
    state: Available,
    objectives: [sample_quest_objective()],
    note: Some("sample_note"),
  }
}

///|
pub fn equal_quest_log_entry(lhs : QuestLogEntry, rhs : QuestLogEntry) -> Bool {
  if lhs.quest_id != rhs.quest_id {
    return false
  }
  if quest_state_to_int(lhs.state) != quest_state_to_int(rhs.state) {
    return false
  }
  if lhs.objectives.length() != rhs.objectives.length() {
    return false
  }
  let mut i_objectives = 0
  while i_objectives < lhs.objectives.length() {
    if !equal_quest_objective(
        lhs.objectives[i_objectives],
        rhs.objectives[i_objectives],
      ) {
      return false
    }
    i_objectives = i_objectives + 1
  }
  match lhs.note {
    None =>
      match rhs.note {
        None => ()
        _ => return false
      }
    Some(left) =>
      match rhs.note {
        Some(right) => if left != right { return false }
        _ => return false
      }
  }
  true
}

///|
pub fn encode_quest_log_entry(value : QuestLogEntry) -> Array[Byte] {
  let writer = @core.Writer::new()
  @core.write_string_field(writer, 1, value.quest_id)
  @core.write_int_field(writer, 2, quest_state_to_int(value.state))
  for item in value.objectives {
    @core.write_message_field(writer, 3, encode_quest_objective(item))
  }
  match value.note {
    Some(v) => @core.write_string_field(writer, 4, v)
    None => ()
  }
  writer.finish()
}

///|
pub fn decode_quest_log_entry(
  input : Array[Byte],
) -> Result[QuestLogEntry, @core.DecodeError] {
  let reader = @core.Reader::new(input)
  let mut quest_id : String = ""
  let mut state : QuestState = Locked
  let objectives : Array[QuestObjective] = []
  let mut note : String? = None
  while !reader.is_empty() {
    match @core.read_key(reader) {
      Err(err) => return Err(err)
      Ok(key) =>
        match key.number {
          1 =>
            match @core.read_string_value(reader, key.wire_type) {
              Err(err) => return Err(err)
              Ok(v) => quest_id = v
            }
          2 =>
            match @core.read_int_value(reader, key.wire_type) {
              Err(err) => return Err(err)
              Ok(v) => state = quest_state_from_int(v)
            }
          3 =>
            match @core.read_message_value(reader, key.wire_type) {
              Err(err) => return Err(err)
              Ok(bytes) =>
                match decode_quest_objective(bytes) {
                  Ok(v) => objectives.push(v)
                  Err(err) => return Err(err)
                }
            }
          4 =>
            match @core.read_string_value(reader, key.wire_type) {
              Err(err) => return Err(err)
              Ok(v) => note = Some(v)
            }
          _ =>
            match @core.skip_field(reader, key.wire_type) {
              Ok(_) => ()
              Err(err) => return Err(err)
            }
        }
    }
  }
  Ok({ quest_id, state, objectives, note })
}
