// Generated by MoonPack. Do not edit by hand.
// Package importing this file should import 001-Elsa/moonpack/src/core as @core.

///|
pub struct QuestObjective {
  objective_id : String
  target_count : Int
  current_count : Int
}

///|
pub fn default_quest_objective() -> QuestObjective {
  { objective_id: "", target_count: 0, current_count: 0 }
}

///|
pub fn sample_quest_objective() -> QuestObjective {
  { objective_id: "sample_objective_id", target_count: 42, current_count: 42 }
}

///|
pub fn equal_quest_objective(
  lhs : QuestObjective,
  rhs : QuestObjective,
) -> Bool {
  if lhs.objective_id != rhs.objective_id {
    return false
  }
  if lhs.target_count != rhs.target_count {
    return false
  }
  if lhs.current_count != rhs.current_count {
    return false
  }
  true
}

///|
pub fn encode_quest_objective(value : QuestObjective) -> Array[Byte] {
  let writer = @core.Writer::new()
  @core.write_string_field(writer, 1, value.objective_id)
  @core.write_int_field(writer, 2, value.target_count)
  @core.write_int_field(writer, 3, value.current_count)
  writer.finish()
}

///|
pub fn decode_quest_objective(
  input : Array[Byte],
) -> Result[QuestObjective, @core.DecodeError] {
  let reader = @core.Reader::new(input)
  let mut objective_id : String = ""
  let mut target_count : Int = 0
  let mut current_count : Int = 0
  while !reader.is_empty() {
    match @core.read_key(reader) {
      Err(err) => return Err(err)
      Ok(key) =>
        match key.number {
          1 =>
            match @core.read_string_value(reader, key.wire_type) {
              Err(err) => return Err(err)
              Ok(v) => objective_id = v
            }
          2 =>
            match @core.read_int_value(reader, key.wire_type) {
              Err(err) => return Err(err)
              Ok(v) => target_count = v
            }
          3 =>
            match @core.read_int_value(reader, key.wire_type) {
              Err(err) => return Err(err)
              Ok(v) => current_count = v
            }
          _ =>
            match @core.skip_field(reader, key.wire_type) {
              Ok(_) => ()
              Err(err) => return Err(err)
            }
        }
    }
  }
  Ok({ objective_id, target_count, current_count })
}
