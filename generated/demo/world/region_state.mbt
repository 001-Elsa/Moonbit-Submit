// Generated by MoonPack. Do not edit by hand.
// Package importing this file should import 001-Elsa/moonpack/src/core as @core.

///|
pub struct RegionState {
  region_id : String
  tiles : Array[TerrainTile]
  npcs : Array[NpcState]
}

///|
pub fn default_region_state() -> RegionState {
  { region_id: "", tiles: [], npcs: [] }
}

///|
pub fn sample_region_state() -> RegionState {
  {
    region_id: "sample_region_id",
    tiles: [sample_terrain_tile()],
    npcs: [sample_npc_state()],
  }
}

///|
pub fn equal_region_state(lhs : RegionState, rhs : RegionState) -> Bool {
  if lhs.region_id != rhs.region_id {
    return false
  }
  if lhs.tiles.length() != rhs.tiles.length() {
    return false
  }
  let mut i_tiles = 0
  while i_tiles < lhs.tiles.length() {
    if !equal_terrain_tile(lhs.tiles[i_tiles], rhs.tiles[i_tiles]) {
      return false
    }
    i_tiles = i_tiles + 1
  }
  if lhs.npcs.length() != rhs.npcs.length() {
    return false
  }
  let mut i_npcs = 0
  while i_npcs < lhs.npcs.length() {
    if !equal_npc_state(lhs.npcs[i_npcs], rhs.npcs[i_npcs]) {
      return false
    }
    i_npcs = i_npcs + 1
  }
  true
}

///|
pub fn encode_region_state(value : RegionState) -> Array[Byte] {
  let writer = @core.Writer::new()
  @core.write_string_field(writer, 1, value.region_id)
  for item in value.tiles {
    @core.write_message_field(writer, 2, encode_terrain_tile(item))
  }
  for item in value.npcs {
    @core.write_message_field(writer, 3, encode_npc_state(item))
  }
  writer.finish()
}

///|
pub fn decode_region_state(
  input : Array[Byte],
) -> Result[RegionState, @core.DecodeError] {
  let reader = @core.Reader::new(input)
  let mut region_id : String = ""
  let tiles : Array[TerrainTile] = []
  let npcs : Array[NpcState] = []
  while !reader.is_empty() {
    match @core.read_key(reader) {
      Err(err) => return Err(err)
      Ok(key) =>
        match key.number {
          1 =>
            match @core.read_string_value(reader, key.wire_type) {
              Err(err) => return Err(err)
              Ok(v) => region_id = v
            }
          2 =>
            match @core.read_message_value(reader, key.wire_type) {
              Err(err) => return Err(err)
              Ok(bytes) =>
                match decode_terrain_tile(bytes) {
                  Ok(v) => tiles.push(v)
                  Err(err) => return Err(err)
                }
            }
          3 =>
            match @core.read_message_value(reader, key.wire_type) {
              Err(err) => return Err(err)
              Ok(bytes) =>
                match decode_npc_state(bytes) {
                  Ok(v) => npcs.push(v)
                  Err(err) => return Err(err)
                }
            }
          _ =>
            match @core.skip_field(reader, key.wire_type) {
              Ok(_) => ()
              Err(err) => return Err(err)
            }
        }
    }
  }
  Ok({ region_id, tiles, npcs })
}
