// Generated by MoonPack. Do not edit by hand.
// Package importing this file should import 001-Elsa/moonpack/src/core as @core.

///|
pub struct PlayerProfile {
  player_id : String
  display_name : String
  position : Vec3
  inventory : Inventory
  skills : Array[SkillProgress]
}

///|
pub fn default_player_profile() -> PlayerProfile {
  {
    player_id: "",
    display_name: "",
    position: default_vec3(),
    inventory: default_inventory(),
    skills: [],
  }
}

///|
pub fn sample_player_profile() -> PlayerProfile {
  {
    player_id: "sample_player_id",
    display_name: "sample_display_name",
    position: sample_vec3(),
    inventory: sample_inventory(),
    skills: [sample_skill_progress()],
  }
}

///|
pub fn equal_player_profile(lhs : PlayerProfile, rhs : PlayerProfile) -> Bool {
  if lhs.player_id != rhs.player_id {
    return false
  }
  if lhs.display_name != rhs.display_name {
    return false
  }
  if !equal_vec3(lhs.position, rhs.position) {
    return false
  }
  if !equal_inventory(lhs.inventory, rhs.inventory) {
    return false
  }
  if lhs.skills.length() != rhs.skills.length() {
    return false
  }
  let mut i_skills = 0
  while i_skills < lhs.skills.length() {
    if !equal_skill_progress(lhs.skills[i_skills], rhs.skills[i_skills]) {
      return false
    }
    i_skills = i_skills + 1
  }
  true
}

///|
pub fn encode_player_profile(value : PlayerProfile) -> Array[Byte] {
  let writer = @core.Writer::new()
  @core.write_string_field(writer, 1, value.player_id)
  @core.write_string_field(writer, 2, value.display_name)
  @core.write_message_field(writer, 3, encode_vec3(value.position))
  @core.write_message_field(writer, 4, encode_inventory(value.inventory))
  for item in value.skills {
    @core.write_message_field(writer, 5, encode_skill_progress(item))
  }
  writer.finish()
}

///|
pub fn decode_player_profile(
  input : Array[Byte],
) -> Result[PlayerProfile, @core.DecodeError] {
  let reader = @core.Reader::new(input)
  let mut player_id : String = ""
  let mut display_name : String = ""
  let mut position : Vec3 = default_vec3()
  let mut inventory : Inventory = default_inventory()
  let skills : Array[SkillProgress] = []
  while !reader.is_empty() {
    match @core.read_key(reader) {
      Err(err) => return Err(err)
      Ok(key) =>
        match key.number {
          1 =>
            match @core.read_string_value(reader, key.wire_type) {
              Err(err) => return Err(err)
              Ok(v) => player_id = v
            }
          2 =>
            match @core.read_string_value(reader, key.wire_type) {
              Err(err) => return Err(err)
              Ok(v) => display_name = v
            }
          3 =>
            match @core.read_message_value(reader, key.wire_type) {
              Err(err) => return Err(err)
              Ok(bytes) =>
                match decode_vec3(bytes) {
                  Ok(v) => position = v
                  Err(err) => return Err(err)
                }
            }
          4 =>
            match @core.read_message_value(reader, key.wire_type) {
              Err(err) => return Err(err)
              Ok(bytes) =>
                match decode_inventory(bytes) {
                  Ok(v) => inventory = v
                  Err(err) => return Err(err)
                }
            }
          5 =>
            match @core.read_message_value(reader, key.wire_type) {
              Err(err) => return Err(err)
              Ok(bytes) =>
                match decode_skill_progress(bytes) {
                  Ok(v) => skills.push(v)
                  Err(err) => return Err(err)
                }
            }
          _ =>
            match @core.skip_field(reader, key.wire_type) {
              Ok(_) => ()
              Err(err) => return Err(err)
            }
        }
    }
  }
  Ok({ player_id, display_name, position, inventory, skills })
}
